// Advent of Code - Day 9

import { part1, part2 } from '../src/day09';

describe('part one tests', () => {
  it('part one test', () => {
    expect(part1([""])).toBe(0);
  });
});

describe('part two tests', () => {
  it('part two test', () => {
    expect(part2([""])).toBe(0);
  });
});
