// Advent of Code - Day <%= data.dayNumber %>

import { part1, part2 } from '../../src/<%= data.year %>/<%= data.dayName %>';

const testData = [
  ""
];

describe('part one tests', () => {
  it('part one test', () => {
    expect(part1(testData)).toBe(0);
  });
});

describe('part two tests', () => {
  it('part two test', () => {
    expect(part2(testData)).toBe(0);
  });
});
